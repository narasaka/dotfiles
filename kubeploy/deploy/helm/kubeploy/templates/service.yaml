apiVersion: v1
kind: Service
metadata:
  name: {{ include "kubeploy.fullname" . }}
  labels:
    {{- include "kubeploy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kubeploy.selectorLabels" . | nindent 4 }}
